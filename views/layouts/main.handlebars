<li>
    {{name}}

    <button class="change-devoured" data-id="{{id}}"
        data-newdevoured="{{devoured}}">
            {{#if devoured}}DEVOURED!{{else}}Not hungry?
            {{/if}}
        </button>
</li>