<h1>Are you hungry for Burgers!</h1>

<h2>Which burger would you like to eat?</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{> layouts.main devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Devoured burgers!</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> layouts.main devoured=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Devour a burger!</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bn">Burger Name:</label>
        <input type="text" id="bn" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">Devour?</label><br>
        <input type="radio" name="devour" value="1" checked> Devour!<br>
        <input type="radio" name="devour" value="0"> Uneaten
    </div>

    <button type="submit">Add Burger</button>
</form>